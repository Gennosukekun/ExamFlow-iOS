# ExamFlow iOS v6.8

Versione iOS di ExamFlow basata sulla v6.8 Natural Scroll. L'app usa WKWebView e include le risorse web locali di ExamFlow.

La build GitHub Actions usa un runner macOS e genera un artifact iOS non firmato per test/build. Per distribuzione su dispositivi reali o App Store è necessaria la firma Apple.